This is the App where all the common models are stored (as a python package) that are being used across the project

To install the package, run:

pip install -e /path/to/app 

for example on our server: pip install -e /opt/eddie/repositories/common_models
